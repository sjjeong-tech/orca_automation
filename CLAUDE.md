# Claude 및 Notion MCP 작업 원칙

향후 Claude와 Notion MCP를 사용할 때 이 문서와 루트의 `AGENTS.md`를 함께 따른다.

## 연결 전

- 사용자가 Notion 연결과 수정 범위를 명시적으로 승인했는지 확인한다.
- 대상 데이터베이스, 페이지, 레코드와 허용 작업을 식별한다.
- 읽기·쓰기 권한을 필요한 최소 범위로 제한한다.
- 민감정보가 프롬프트, 로그, Notion 속성에 들어가지 않도록 점검한다.

## 읽기 및 판단

- Notion 기록은 운영 상태의 기준으로 사용하되, 실제 파일 내용은 연결된 Google Drive 자료로 확인한다.
- 확인되지 않은 사실을 채우거나 빈 값을 추정하지 않는다.
- 상태 변경의 근거와 다음 행동을 함께 확인한다.
- 충돌하는 정보가 있으면 덮어쓰지 말고 사용자 확인 대상으로 표시한다.

## 쓰기

- 동일 Notion 레코드의 최종 쓰기 Agent를 작업 시작 전에 한 명으로 지정한다.
- 쓰기 전에 변경 대상, 이전 값, 새 값을 검토한다.
- 최소 Tracker 속성과 정의된 상태값만 우선 사용한다.
- 일괄 수정은 소규모 Pilot으로 검증한 뒤 확대한다.
- 실패나 부분 성공 시 재시도 전에 실제 반영 결과를 다시 읽어 중복 수정을 방지한다.
- 실제 제출본이나 증빙을 Notion에 복제하지 않고 Drive 경로로 연결한다.

## 금지 사항

- 승인 없는 데이터베이스 생성·수정·삭제
- 민감정보 기록
- 근거 없는 완료 처리 또는 담당자 지정
- 여러 Agent의 동일 레코드 동시 쓰기
- 실제 Notion 상태와 검증 없이 자동화 결과를 성공으로 간주하는 행위
