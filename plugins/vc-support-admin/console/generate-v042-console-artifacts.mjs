#!/usr/bin/env node
import { mkdir, writeFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { DEMO_REQUESTS, buildConsolePreview, buildGapResolutionPreview } from "./admin-process-console-server.mjs";

const here = path.dirname(fileURLToPath(import.meta.url));
const reportsDir = path.resolve(here, "../reports/console-pilot");
const prefix = "request-task-console-v0.4.2";
const safe = (value) => JSON.stringify(value).replace(/</g, "\\u003c").replace(/>/g, "\\u003e").replace(/&/g, "\\u0026");

function html(data) {
  return `<!doctype html><html lang="ko"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Notion Mapping Gap Preview v0.4.2</title><style>
  :root{--p:#49317b;--ink:#211b31;--muted:#6d667a;--line:#ddd7e6;--bg:#f7f5fa;--danger:#9b2943}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:14px Pretendard,Inter,Arial,sans-serif}.shell{max-width:1440px;margin:auto;padding:24px}.card{background:#fff;border:1px solid var(--line);border-radius:16px;padding:18px;box-shadow:0 7px 22px #39245a0d}.head{display:flex;justify-content:space-between;gap:20px;background:linear-gradient(135deg,#fff,#f0ebfb)}h1,h2,h3{margin:.2em 0}.eyebrow{color:var(--p);font-size:12px;font-weight:800;letter-spacing:.08em}.muted{color:var(--muted)}.badge{display:inline-block;padding:5px 9px;border-radius:999px;background:#eee8f7;color:var(--p);font-weight:800}.danger{color:var(--danger);font-weight:800}.layout{display:grid;grid-template-columns:280px 1fr;gap:16px;margin-top:16px}.inbox button,.choice button{display:block;width:100%;text-align:left;margin:8px 0;padding:11px;border:1px solid var(--line);background:#fff;border-radius:10px;cursor:pointer}.inbox button.selected,.choice button.selected{border-color:var(--p);box-shadow:0 0 0 2px #49317b22}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px;margin:16px 0}.fact{display:grid;grid-template-columns:1fr auto;gap:8px;padding:6px 0;border-bottom:1px solid #eee}.choice small{display:block;color:var(--muted);margin-top:4px}.table{overflow:auto}table{border-collapse:collapse;width:100%;font-size:12px}th,td{border:1px solid var(--line);padding:7px;text-align:left;vertical-align:top}.footer{margin-top:16px}@media(max-width:900px){.layout,.grid{grid-template-columns:1fr}.head{display:block}}
  </style><body><main class="shell"><header class="card head"><div><p class="eyebrow">LOCAL DECISION PREVIEW · READ ONLY</p><h1>Notion 반영 준비 해결안</h1><p class="muted">후보를 선택해도 Notion·Schema·Record에는 저장되지 않습니다.</p></div><div><span class="badge">WRITE NOT EXECUTED</span><p>Notion Write 0 · Operational Write 0</p></div></header><div class="layout"><aside class="card inbox"><p class="eyebrow">REQUEST INBOX</p><div id="inbox"></div></aside><section><article class="card"><p class="eyebrow">CURRENT REQUEST</p><h2 id="request-title"></h2><div id="summary"></div></article><div class="grid"><article class="card"><p class="eyebrow">FUND RELATION</p><div id="fund"></div></article><article class="card"><p class="eyebrow">DURABLE DUPLICATE</p><div id="duplicate"></div></article><article class="card"><p class="eyebrow">COMPOSITE PROCESS</p><div id="composite"></div></article></div><article class="card"><p class="eyebrow">DECISION PREVIEW</p><h2>사용자 선택 (로컬 상태)</h2><div id="choices" class="grid"></div><div id="decision" class="footer"></div></article><article class="card"><p class="eyebrow">CANDIDATE COMPARISON</p><div class="table"><table><thead><tr><th>Gap</th><th>Candidate</th><th>Existing structure</th><th>Schema change</th><th>Write</th><th>Recommendation</th></tr></thead><tbody id="comparison"></tbody></table></div></article><details class="card footer"><summary>기술 상세 JSON</summary><pre id="raw"></pre></details></section></div></main><script>const DATA=${safe(data)};let id='REQ-DEMO-001',decisions={fund_relation:'DECISION_PENDING',durable_duplicate:'DECISION_PENDING',composite_process:'DECISION_PENDING'};const $=s=>document.querySelector(s);const esc=v=>String(v??'-').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));const rows=a=>a.map(([k,v])=>'<div class="fact"><span>'+esc(k)+'</span><strong>'+esc(v)+'</strong></div>').join('');function render(){const x=DATA[id],p=x.preview,g=x.gap,d=p.demo_request;$('#inbox').innerHTML=Object.values(DATA).map(v=>{const q=v.preview.demo_request;return '<button class="'+(q.request_id===id?'selected':'')+'" data-id="'+q.request_id+'"><strong>'+q.request_id+'</strong><small>'+esc(q.title)+'</small><small>'+q.dummy_fund_id+' · '+q.scenario_id+'</small></button>'}).join('');$('#request-title').textContent=d.title;$('#summary').innerHTML=rows([['Fund',p.request.dummy_fund_id],['Scenario',p.scenario_id],['Transaction',p.request.transaction_id],['Approval Status',g.approval_status],['Test Write Readiness',g.test_write_readiness],['Blocking gaps',g.blocking_gap_count]]);$('#fund').innerHTML=rows([['Candidate DB',g.fund_relation.candidate_db],['Exact match count',g.fund_relation.match_count],['Recommendation',g.fund_relation.recommendation],['Approval',g.fund_relation.required_approval]]);$('#duplicate').innerHTML=rows([['Result',g.durable_duplicate_result],['Recommendation','NEW_PROPERTY_REQUIRED'],['Existing fallback','테스트 케이스 ID / title prefix'],['Approval','전용 Property 생성 승인']]);$('#composite').innerHTML=rows([['Recommendation',g.composite_process_recommendation],['Request Process','NOT_WRITTEN for composite'],['Task Process','P03 · P04 · P07'],['Data loss',g.composite_data_loss_result]]);$('#choices').innerHTML=[['fund_relation','조합 연결','별도 TEST 구조 필요'],['durable_duplicate','중복 방지','전용 Transaction ID 필요'],['composite_process','복합 업무','Task별 Process를 source of truth로 사용']].map(([k,l,v])=>'<div class="choice"><h3>'+l+'</h3><button data-choice="'+k+'" class="'+(decisions[k]!=='DECISION_PENDING'?'selected':'')+'">'+v+'<small>현재: '+decisions[k]+'</small></button></div>').join('');const remaining=Object.values(decisions).filter(v=>v==='DECISION_PENDING').length;$('#decision').innerHTML='<p class="danger">Required Write: '+(decisions.fund_relation==='DECISION_PENDING'||decisions.durable_duplicate==='DECISION_PENDING'?'YES':'USER APPROVAL STILL REQUIRED')+'</p><p>Remaining Blocking Gaps: '+remaining+' · Test Write Ready: TEST_WRITE_BLOCKED · Write Count: 0</p>';$('#comparison').innerHTML=g.candidate_comparison.map(r=>'<tr><td>'+esc(r.gap)+'</td><td>'+esc(r.candidate)+'</td><td>'+esc(r.existing_structure_reused)+'</td><td>'+esc(r.schema_change_required)+'</td><td>'+esc(r.write_required)+'</td><td>'+esc(r.recommendation)+'</td></tr>').join('');$('#raw').textContent=JSON.stringify({preview:p,gap_analysis:g,local_decisions:decisions},null,2);window.__consoleReady=true;window.__consoleDiagnostics={selectedRequestId:id,decisions,writeCount:0};}$('#inbox').addEventListener('click',e=>{const b=e.target.closest('button[data-id]');if(b){id=b.dataset.id;decisions={fund_relation:'DECISION_PENDING',durable_duplicate:'DECISION_PENDING',composite_process:'DECISION_PENDING'};render()}});$('#choices').addEventListener('click',e=>{const b=e.target.closest('button[data-choice]');if(b){decisions[b.dataset.choice]='SELECTED_LOCAL_ONLY';render()}});render();</script></body></html>`;
}

export async function generateV042Artifacts() {
  await mkdir(reportsDir, { recursive: true });
  const payloads = Object.fromEntries(await Promise.all(DEMO_REQUESTS.map(async (demo) => {
    const preview = await buildConsolePreview({ request_id: demo.request_id, scenario_id: demo.scenario_id, request_text: demo.request_text });
    return [demo.request_id, { preview, gap: await buildGapResolutionPreview({ request_id: demo.request_id, scenario_id: demo.scenario_id, preview_output: preview }) }];
  })));
  const selected = payloads["REQ-DEMO-001"];
  const output = { console_version: "PILOT v0.4.2", selected_request: "REQ-DEMO-001", demo_requests: DEMO_REQUESTS, request: selected.preview.request, gap_analysis: selected.gap, payloads, notion_write_count: 0, operational_write_count: 0 };
  const evidence = { schema_read_at_kst: selected.gap.schema_read_at_kst, request_db: "[TEST LAB] 지원팀 업무요청", task_db: "[TEST LAB] 지원팀 Task", fund_target_db: "[TEST LAB] 전체관리조합", fund_relation_candidates: selected.gap.fund_relation, transaction_candidates: selected.gap.transaction_candidates, composite_process_candidates: selected.gap.composite_process_candidates, candidate_comparison: selected.gap.candidate_comparison, recommendations: { fund_relation: selected.gap.fund_relation.recommendation, transaction: "NEW_PROPERTY_REQUIRED", composite: selected.gap.composite_process_recommendation }, required_approvals: selected.gap.required_approvals, blocking_gaps: selected.gap.blocking_gaps, notion_write_count: 0, operational_write_count: 0 };
  const guide = `# Notion Mapping Gap Resolution Preview v0.4.2\n\n## 목적\n\n기존 TEST LAB Schema와 Exact-match 조회 결과를 읽기 전용으로 비교합니다. 이 화면의 선택은 로컬 Preview 상태이며 Notion Write를 수행하지 않습니다.\n\n## 실행\n\n\`node plugins/vc-support-admin/console/admin-process-console-server.mjs\`\n\n독립 시연은 \`${prefix}.html\`을 직접 엽니다.\n\n## 판단\n\n- Dummy Fund A/B/E Exact Match는 0입니다. 안전한 Relation에는 별도 TEST Dummy Fund Record 승인·생성이 필요합니다.\n- 전용 Transaction ID Property가 없어 durable duplicate protection은 준비되지 않았습니다.\n- COMPOSITE-01은 Request Process를 비우고 Task별 Process ID와 상위 요청 Relation을 source of truth로 쓰는 후보가 기존 구조를 가장 적게 변경합니다.\n- 추가 Query Data Source는 workspace usage limit으로 미실행이므로 해당 후보는 확인 필요로 표시합니다.\n\nNotion Write 0 · Operational Write 0\n`;
  await Promise.all([writeFile(path.join(reportsDir, `${prefix}.html`), html(payloads), "utf8"),writeFile(path.join(reportsDir, `${prefix}-output.json`), JSON.stringify(output, null, 2)+"\n", "utf8"),writeFile(path.join(reportsDir, `${prefix}-gap-analysis.json`), JSON.stringify(evidence, null, 2)+"\n", "utf8"),writeFile(path.join(reportsDir, `${prefix}-demo.md`), guide, "utf8")]);
  return { payloads, output, evidence };
}
if (process.argv[1] && path.resolve(process.argv[1]) === fileURLToPath(import.meta.url)) { const result = await generateV042Artifacts(); console.log(`console-v0.4.2-artifacts: PASS (${Object.keys(result.payloads).length} requests, write=0)`); }
