# Replay examples

`SINGLE-P03-01` demonstrates a completion candidate only. `SINGLE-P03-02` demonstrates a missing stamped original, a separate human question, and a real blocker. `COMPOSITE-01` preserves P03 and P04 while P07 alone is blocked for missing submission material.

All examples use the fixtures in `plugins/vc-support-admin/fixtures/` and must include `--preview`.
